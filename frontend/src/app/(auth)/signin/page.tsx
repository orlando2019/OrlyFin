import { SignInForm } from "@/modules/auth-users/components/signin-form";

// Renderiza la página 'sign in page' y compone su contenido principal.
export default function SignInPage() {
  return <SignInForm />;
}
