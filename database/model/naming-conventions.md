# Naming Conventions

- Tablas en snake_case plural (`credit_cards`).
- PK: `id` (UUID textual o nativo segun decision de implementacion).
- FK: `<entity>_id`.
- Timestamps: `created_at`, `updated_at`.
- Auditoria opcional: `created_by`, `updated_by`.
